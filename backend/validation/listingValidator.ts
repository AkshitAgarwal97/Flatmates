// Validation is now handled directly in the controller
// This file is kept for reference but not actively used
// If joi validation is needed, install: npm install joi @types/joi

export const getListingsSchema = {
    // Schema definition kept for reference
    // Actual validation happens in controller with type checking
};
