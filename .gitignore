# Environment files
.env
.env.*
backend/.env
backend/.env.*
frontend/.env
frontend/.env.*
*.env

# !! Master secrets file — NEVER commit this !!
SECRETS.env

# Keys and Secrets
*.pem
secret.*
flatmates-key.pem

# Build and cache
node_modules/
.cache/
*.pack
.DS_Store
deploy.tar.gz

# Tool specific
.github/instructions/kluster-code-verify.instructions.md
.kluster
.vercel

# !! MongoDB local data — NEVER commit raw DB files !!
backend/mongodb_data/
mongodb_data/
*.wt
*.turtle
WiredTiger*
mongod.lock

# Built output — generated on deploy, not needed in repo
backend/dist/
frontend/build/

# Local uploads — use Cloudinary in production
backend/uploads/

# Logs
*.log
npm-debug.log*
